# 📌 Setup Required - إعداد مطلوب

## ⚠️ خطوة مهمة قبل استخدام خيار "Otro Dispositivo"

قبل أن تتمكن من استخدام خيار **"Otro Dispositivo no encontrado"** في صفحة الإصلاحات، يجب تشغيل ملف setup لمرة واحدة فقط.

### 🚀 خطوات التشغيل:

1. افتح المتصفح واذهب إلى:
   ```
   http://localhost/RepairPoint/setup_default_brand.php
   ```

2. انتظر حتى يكتمل Setup بنجاح ✅

3. ستظهر لك رسالة **"Migration completado exitosamente!"**

4. الآن يمكنك استخدام خيار **"Otro Dispositivo"** في صفحة:
   ```
   http://localhost/RepairPoint/pages/add_repair.php
   ```

---

## 📝 ما يفعله هذا Setup؟

- ✅ يضيف ماركة افتراضية: **"Desconocido"**
- ✅ يضيف موديل افتراضي: **"Dispositivo Personalizado"**
- ✅ يحفظ الإعدادات في قاعدة البيانات

هذا يسمح لك بإضافة أجهزة مخصصة (غير موجودة في القائمة) دون أخطاء في قاعدة البيانات.

---

## ❓ ملاحظات:

- ⚙️ **يُنفذ مرة واحدة فقط** - لا حاجة لتشغيله مرة أخرى
- 📊 **لن يتأثر Dashboard والإحصائيات** - ستعمل بشكل طبيعي
- ✅ **الإصلاحات القديمة لن تتأثر** - فقط الإصلاحات الجديدة مع خيار "Otro"
- 🔍 **في الإحصائيات**: الأجهزة المخصصة ستظهر كـ "Desconocido" في إحصائيات الماركات
- 📱 **في التفاصيل**: سيظهر الاسم الحقيقي المخصص (مثل: "Huawei Nova 12 Pro")

---

## 🆘 إذا واجهت مشاكل:

إذا لم يعمل Setup بشكل صحيح، تأكد من:
1. ✅ قاعدة البيانات تعمل
2. ✅ لديك صلاحية الوصول
3. ✅ تحقق من أخطاء PHP

---

**💡 نصيحة**: بعد إكمال Setup، يمكنك حذف ملف `setup_default_brand.php` لأمان أفضل.
