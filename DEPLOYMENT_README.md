# 🚀 RepairPoint - نشر سريع

## 📌 ملخص سريع

تم إعداد جميع ملفات النشر على السيرفر `rp.electromti.com`.

## 🎯 ملفات النشر الجديدة

1. **`config/database.production.php`** - إعدادات قاعدة البيانات للإنتاج
2. **`deploy.sh`** - سكريبت رفع تلقائي
3. **`.htaccess.production`** - إعدادات Apache محسّنة للإنتاج
4. **`api/test_connection.php`** - اختبار الاتصال بقاعدة البيانات
5. **`DEPLOYMENT_GUIDE.md`** - دليل النشر الكامل

## ⚡ طريقة النشر السريعة

### الخطوة 1: استخدم السكريبت التلقائي

```bash
./deploy.sh
```

### الخطوة 2: على السيرفر

```bash
# اتصل بالسيرفر
ssh u948397987@fr-int-web2000.user.fm

# انتقل للمجلد
cd /home/u948397987/domains/rp.electromti.com/public_html

# نسخ ملف الإنتاج
cp config/database.production.php config/database.php

# تعديل بيانات قاعدة البيانات
nano config/database.php

# نسخ htaccess للإنتاج
cp .htaccess.production .htaccess

# تعيين الصلاحيات
chmod 777 logs/
chmod 777 assets/uploads/
```

### الخطوة 3: اختبار

افتح في المتصفح:
```
https://rp.electromti.com/api/test_connection.php
```

يجب أن ترى:
```json
{
  "status": "success",
  "message": "اتصال قاعدة البيانات ناجح"
}
```

### الخطوة 4: الأمان

```bash
# احذف ملف الاختبار
rm -f api/test_connection.php
```

## 📋 معلومات قاعدة البيانات

```
Host: localhost
Database: u948397987_repairpoint
Username: u948397987_repair
Password: [غيّرها في config/database.php]
```

## 🔗 روابط مفيدة

- **الموقع**: https://rp.electromti.com
- **cPanel**: [رابط لوحة التحكم]
- **phpMyAdmin**: [رابط phpMyAdmin]

## 📚 لمزيد من التفاصيل

اقرأ **DEPLOYMENT_GUIDE.md** للحصول على دليل شامل مفصّل.

## ✅ قائمة المهام

- [ ] رفع الملفات إلى السيرفر
- [ ] تعديل `config/database.php`
- [ ] تعديل `.htaccess`
- [ ] استيراد قاعدة البيانات
- [ ] اختبار الاتصال
- [ ] إنشاء مستخدم مدير
- [ ] حذف ملفات الاختبار
- [ ] تفعيل SSL
- [ ] اختبار جميع الوظائف

---

**تاريخ الإنشاء**: 2026-01-12
**الإصدار**: 1.0
